# 1. INSTALL MONGODB (UBUNTU / KALI)
# ==========================================
sudo apt update
sudo apt install mongodb-org

# Start MongoDB Service
sudo systemctl start mongod

# Enable MongoDB at boot
sudo systemctl enable mongod
=
# 2. OPEN MONGODB SHELL


mongosh



# 3. CREATE / SWITCH DATABASE

use dbms_lab

4. THen code 


